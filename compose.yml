services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - "${APP_HOST_PORT:-8080}:${APP_PORT:-8080}"
    environment:
      DB_HOST: mysql
    depends_on:
      mysql:
        condition: service_healthy

  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: "${DB_PASSWORD:-root}"
      MYSQL_DATABASE: "${DB_NAME:-mecanica}"
    ports:
      - "${DB_HOST_PORT:-3306}:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -uroot -p$$MYSQL_ROOT_PASSWORD"]
      interval: 5s
      timeout: 5s
      retries: 10

  sonarqube:
    image: sonarqube:community
    env_file:
      - .env
    ports:
      - "${SONAR_HOST_PORT:-9000}:9000"
    environment:
      SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
    volumes:
      - sonar_data:/opt/sonarqube/data
      - sonar_extensions:/opt/sonarqube/extensions
      - sonar_logs:/opt/sonarqube/logs
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9000/api/system/status | grep -q '\"status\":\"UP\"'"]
      interval: 10s
      timeout: 5s
      retries: 30

  sonar-scanner:
    image: sonarsource/sonar-scanner-cli
    profiles:
      - tools
    env_file:
      - .env
    environment:
      SONAR_HOST_URL: "${SONAR_HOST_URL:-http://sonarqube:9000}"
    volumes:
      - .:/usr/src
    depends_on:
      sonarqube:
        condition: service_healthy

  zap:
    image: zaproxy/zap-stable
    profiles:
      - tools
    volumes:
      - ./security/reports:/zap/wrk:rw
      - ./security/zap-rules.tsv:/zap/wrk/zap-rules.tsv:ro
    entrypoint: ["sh", "-c"]
    command:
      - >
        zap-api-scan.py -t http://app:${APP_PORT:-8080}/swagger/doc.json -f openapi
        -r ${ZAP_REPORT_PREFIX:-zap-report}.html -J ${ZAP_REPORT_PREFIX:-zap-report}.json
        -c zap-rules.tsv
        -z "-config replacer.full_list(0).description=auth
        -config replacer.full_list(0).enabled=true
        -config replacer.full_list(0).matchtype=REQ_HEADER
        -config replacer.full_list(0).matchstr=Authorization
        -config replacer.full_list(0).regex=false
        -config replacer.full_list(0).replacement=Bearer\ ${ZAP_TOKEN}"

volumes:
  mysql_data:
  sonar_data:
  sonar_extensions:
  sonar_logs:
