services:
  mysql-dast:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: "${DB_PASSWORD:-root}"
      MYSQL_DATABASE: "${DB_NAME:-mecanica}"
    ports:
      - "${DAST_DB_HOST_PORT:-3307}:3306"
    tmpfs:
      - /var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -uroot -p$$MYSQL_ROOT_PASSWORD"]
      interval: 5s
      timeout: 5s
      retries: 10

  app-dast:
    build: ..
    env_file:
      - ../.env
    ports:
      - "${DAST_APP_HOST_PORT:-8081}:${APP_PORT:-8080}"
    environment:
      DB_HOST: mysql-dast
    depends_on:
      mysql-dast:
        condition: service_healthy

  zap:
    image: zaproxy/zap-stable
    profiles:
      - tools
    volumes:
      - ./reports:/zap/wrk:rw
      - ./zap-rules.tsv:/zap/wrk/zap-rules.tsv:ro
    entrypoint: ["sh", "-c"]
    command:
      - >
        zap-api-scan.py -t http://app-dast:${APP_PORT:-8080}/swagger/doc.json -f openapi
        -r ${ZAP_REPORT_PREFIX:-zap-report}.html -J ${ZAP_REPORT_PREFIX:-zap-report}.json
        -c zap-rules.tsv
        -z "-config replacer.full_list(0).description=auth
        -config replacer.full_list(0).enabled=true
        -config replacer.full_list(0).matchtype=REQ_HEADER
        -config replacer.full_list(0).matchstr=Authorization
        -config replacer.full_list(0).regex=false
        -config replacer.full_list(0).replacement=Bearer\ ${ZAP_TOKEN}"
